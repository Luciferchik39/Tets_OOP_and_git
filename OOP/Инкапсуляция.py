"""
===========      ИНКАПСУЛЯЦИЯ — сокрытие внутреннего устройства     ================

Важный технический момент
Когда вы пишете anna.describe(), Python делает за вас магию:
Он берет объект anna.
Он берет функцию describe из класса Person.
Он неявно передает anna в качестве первого аргумента (self) в эту функцию.
Фактически выполняется Person.describe(anna).
Почему это важно? (Инкапсуляция)
Методы реализуют принцип инкапсуляции — объединение данных (атрибутов) и действий (методов) в одной сущности (объекте).
Объект сам отвечает за свои действия.
Внешнему коду не нужно знать, как объект что-то делает (например, как именно считается возраст). Нужно только знать,
что он умеет делать (его публичные методы).
Данные объекта часто защищены от прямого внешнего изменения — меняться они должны через его же методы.
Например, правильнее вызвать anna.have_birthday(), чем делать anna.age += 1 напрямую.
"""


class EncapsulationDemo:
    # 🔧 ИНСТРУМЕНТ 1: Приватные атрибуты (__)
    def __init__(self):
        self.__secret = "🤫 Только для внутреннего использования"
        self._protected = "⚠️ Только для наследников"

    # 🔧 ИНСТРУМЕНТ 2: Геттеры/Сеттеры
    def get_secret(self):  # Контролируемый доступ
        return self.__secret

    def set_secret(self, value):  # Валидация при установке
        if len(value) > 3:
            self.__secret = value

    # 🔧 ИНСТРУМЕНТ 3: Property декораторы
    @property
    def protected(self):  # Геттер в стиле Python
        return self._protected

    @protected.setter
    def protected(self, value):  # Сеттер в стиле Python
        self._protected = value

    # 🔧 ИНСТРУМЕНТ 4: Приватные методы
    def __internal_logic(self):  # Нельзя вызвать снаружи
        return "Сложные вычисления"

    def public_api(self):  # Публичный интерфейс
        return self.__internal_logic()