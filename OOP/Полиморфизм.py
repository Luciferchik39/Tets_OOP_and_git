"""
==============    ПОЛИМОРФИЗМ — многообразие форм  ============

"""


# 🔧 ИНСТРУМЕНТ 1: Переопределение методов (Override)
class Bird:
    def sound(self):
        return "Чирик"


class Parrot(Bird):
    def sound(self):  # Переопределяем!
        return "Привет!"


class Crow(Bird):
    def sound(self):  # Переопределяем по-своему!
        return "Кар!"


# 🔧 ИНСТРУМЕНТ 2: Перегрузка через *args, **kwargs
class Calculator:
    def add(self, *args):  # Один метод для разного количества аргументов
        return sum(args)


# 🔧 ИНСТРУМЕНТ 3: Duck Typing (утиная типизация)
def make_sound(animal):  # Не важно КТО, важно ЧТО УМЕЕТ
    return animal.sound()  # Лишь бы был метод sound()!


# 🔧 ИНСТРУМЕНТ 4: Магические методы (__method__)
class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):  # Полиморфизм оператора +
        return Vector(self.x + other.x, self.y + other.y)

    def __str__(self):  # Полиморфизм преобразования в строку
        return f"({self.x}, {self.y})"


# 🔧 ИНСТРУМЕНТ 5: Протоколы (коллекции, итераторы)
class Playlist:
    def __init__(self):
        self.songs = []

    def __len__(self):  # Протокол коллекции
        return len(self.songs)

    def __getitem__(self, index):  # Протокол последовательности
        return self.songs[index]

    def __iter__(self):  # Протокол итератора
        return iter(self.songs)