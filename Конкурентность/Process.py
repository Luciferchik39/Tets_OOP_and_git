"""
❌ Прямого аналога os.fork() НЕТ
❌ Нельзя создать процесс, копирующий состояние родителя
❌ Нельзя получить экспоненциальный рост процессов (2ⁿ)

✅ Используй multiprocessing.Process (это spawn)
✅ Передавай данные через args или Queue
✅ Для fork-поведения - ставь WSL или используй Linux

ГЛАВНОЕ ОТЛИЧИЕ:
fork = "клонируй меня полностью"
spawn = "запусти новую копию с нуля
time.sleep() блокирует всё (в обычном коде)
await asyncio.sleep() не блокирует

Недостатки:
1. Высокие накладны расходы, за счёт того что под капотом придётся заводить новый интерпритатор и прогружать туда
все исполняемы можули
2. процессы имеют изолированные области памяти, поэтому для общения между ними придётся сооружать костыли

РАСХОДЫ В MULTIPROCESSING:

⏱️ ПО ВРЕМЕНИ (CPU):
- Создание нового процесса (обращение к ОС)
- Запуск нового интерпретатора Python
- Импорт всех модулей заново
- Сериализация/десериализация данных

💾 ПО ПАМЯТИ (RAM):
- Каждый процесс - отдельный интерпретатор (~30-50 MB)
- Каждый процесс - копия всех импортированных модулей
- Каждый процесс - копия данных (если не использовать shared memory)

📊 НАГЛЯДНО:
Если программа весит 200 MB и создает 4 процесса:
200 MB × 4 = 800 MB + родитель = 1 GB!

🛠️ КАК БОРОТЬСЯ:
1. Использовать Pool() вместо создания новых процессов
2. Импортировать тяжелые библиотеки только где нужно
3. Использовать shared memory для общих данных
4. На Windows - быть готовым к расходам (это нормально!)
"""

import os
from multiprocessing import Process
import time

CNT = 0
print(f"🔍 Код ВНЕ if __name__ выполняется!")
print(f"   __name__ = {__name__}")
print(f"   PID = {os.getpid()}")
print("-" * 30)


def funck():
    global CNT
    print(f"   👉 Функция запущена в PID: {os.getpid()}")
    print(f"   👉 __name__ внутри функции = {__name__}")
    time.sleep(1)
    CNT += 2
    print(f"   👉 CNT = {CNT}, PID: {os.getpid()}")


if __name__ == '__main__':
    print(f"🚀 Родительский процесс PID: {os.getpid()}")
    print(f"   CNT в начале = {CNT}")

    p = Process(target=funck)
    p.start()
    p.join()

    p_2 = Process(target=funck)
    p_2.start()
    p_2.join()

    print(f"🏁 CNT в конце = {CNT} (в родителе)")