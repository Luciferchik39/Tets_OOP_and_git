"""
Асинхронность (asyncio) — это способ организации кода, при котором программа может приостанавливать выполнение
текущей задачи в момент ожидания (например, ответа от сервера или чтения файла) и переключаться на выполнение других
задач. Это не многозадачность в смысле параллельного исполнения (там используется многопоточность), а именно кооперативная многозадачность.

============================     Где используется?
Веб-серверы (FastAPI, aiohttp).
Парсеры, которые качают много страниц.
Телеграм-боты (aiogram).
Работа с базами данных (asyncpg, databases, SQLAlchemy 1.4+)

============================     Блок 1: Фундамент. Event Loop и базовые конструкции
Здесь мы просто учимся пользоваться инструментами, не вдаваясь в глубины.
async def: Как пометить функцию, что она асинхронная (корутина).
await: Сказать программе: «Здесь нужно подождать результата, иди пока поделай другие дела».
asyncio.run(): Главный пусковой кран, который запускает "главный" асинхронный блок.
asyncio.sleep(): Аналог time.sleep(), но неблокирующий (главная демка).

============================     Блок 2: Задачи (Tasks) — как запускать несколько штук сразу
Если мы просто напишем await func1() и потом await func2(), они выполнятся по очереди. Чтобы они работали "одновременно" (конкурентно), нужны задачи.
asyncio.create_task(): Обернуть вызов функции в задачу, чтобы она начала выполняться в фоне.
asyncio.gather(): Самый популярный инструмент. Говорит: «Запустить вот этот список функций конкурентно и подождать, пока все закончат».

============================     Блок 3: Ожидание и таймауты
В реальном мире сеть падает или тормозит. Нужно уметь контролировать время.
asyncio.wait_for(): Ждать выполнения задачи, но не дольше N секунд (иначе ошибка).
asyncio.timeout(): Контекстный менеджер для ограничения времени выполнения блока кода (Python 3.11+).
asyncio.shield(): Защита задачи от отмены (даже если сработал таймаут снаружи, пусть задача доработает в фоне).

============================     Блок 4: Асинхронные итераторы и контекстные менеджеры
Синтаксический сахар для удобной работы.
Асинхронный менеджер контекста (async with): Используется для открытия соединений (сессия aiohttp ClientSession, подключение к БД). Гарантирует правильное закрытие.
Асинхронный генератор (async for): Нужен, когда данные приходят не сразу, а частями (например, чтение строк из большого лога или курсор в БД).

============================     Блок 5: Синхронизация и очереди (Продвинутый уровень)
Если задачи обмениваются данными или нужна строгая очередность.
asyncio.Queue(): Потокобезопасная (вернее, корутинобезопасная) очередь. Классика: продюсеры кладут задачи, воркеры забирают и выполняют.
asyncio.Semaphore(): Ограничитель количества одновременных запросов (чтобы не забанили на сайте или не положить сервер).
asyncio.Lock(): Блокировка, чтобы две корутины не изменили одну переменную одновременно (гонка данных).
"""

"Синхронно = медленно"
import asyncio
from datetime import datetime

# def fetch_data(delay, name):
#     print(f'начало {name}')
#     time.sleep(delay) # имитация действия и ожидания между запосом и ответом
#     print(f'функция выполнилась рузультат {name}')
#
# if __name__ == '__main__':
#     start = datetime.now()
#
#     fetch_data(2, "задача 1")
#     fetch_data(2, "задача 2")
#     print(f'время работы процесса: {datetime.now() - start}')

"Асинхронно (быстро)"
""
async def fetch_data(delay, name):
    print(f'начало {name}')
    await asyncio.sleep(delay) # имитация действия и ожидания между запосом и ответом и в этот момент asyncio.sleep позволяет освободитя gil
    print(f'функция выполнилась рузультат {name}')

async def my_fn():
    # Создаем две задачи, которые будут выполняться конкурентно
    task_one = asyncio.create_task(fetch_data(2, "задача 1"))
    task_two = asyncio.create_task(fetch_data(2, "задача 2"))
    # Ждем выполнения обеих задач
    """Ты говоришь: "Я сейчас встану здесь на паузу и буду ждать 5 секунд. Никуда не пойду дальше, пока эти 5 секунд не пройдут".
Контекст: Ты не можешь делать ничего другого в этот момент в рамках текущей корутины. Ты просто ждешь."""
    res_1 = await task_one
    res_2 = await task_two

    return res_1, res_2

async def slow_fn():
    """Корутина "засыпает" на объекте, который умеет ждать (awaitable).
Если упростить: она встречает ключевое слово await и говорит Event Loop'у: "Вот этот объект я должна подождать. Забери
у меня управление, пока он не будет готов". Event Loop уходит делать другие задачи, а когда объект (например, сокет или
таймер) сигнализирует "я готов", loop будит корутину, и она продолжается со строки после await."""
    print('первая медленная функция на 10 сек запущена')
    await asyncio.sleep(10)
    print('первая медленная функция завершилась')

async def slow_fn_2():
    print('вторая медленная функция на 10 сек запущена')
    await asyncio.sleep(10)
    print('вторая медленная функция завершилась')

async def fast_fn():
    print('первая быстрая функция на 1 сек запущена')
    await asyncio.sleep(1)
    print('первая быстрая функция завершилась')

async def fast_fn_2():
    print('вторая быстрая функция на 1 сек запущена')
    await asyncio.sleep(1)
    print('вторая быстрая функция завершилась')

async def my_fn_task():
    """
    Ты говоришь Event Loop'у: "Возьми эту корутину (fast_fn_2()) и запланируй её выполнение в фоне. Она сейчас начнет
    тикать сама по себе. А я (текущая корутина) пойду дальше по коду, не дожидаясь этих 5 секунд".Нюанс: Если ты после
    create_task не сделаешь await task, то программа может завершиться, не дождавшись окончания задачи (потому что main
    закончился, а фоновая задача не успела). Чтобы получить результат или просто дождаться фоновой работы, тебе всё
    равно потом нужно написать await task.
    """
    task_four = asyncio.create_task(fast_fn_2())
    task_three = asyncio.create_task(fast_fn())
    task_one = asyncio.create_task(slow_fn())
    task_two = asyncio.create_task(slow_fn_2())

    "await внутри одной корутины строго последовательны, но это нормально, потому что остальные задачи работают параллельно в фоне! 👍"
    res_3 = await task_three
    res_4 = await task_four
    res_1 = await task_one
    res_2 = await task_two



if __name__ == '__main__':
    start = datetime.now()
    """asyncio.run(main()). Эта команда делает три вещи: создает новый Event Loop, запускает в нем твою корутину, а 
    после завершения закрывает loop. Это главная точка входа."""
    asyncio.run(my_fn_task())

    # asyncio.run(my_fn())
    # a = asyncio.run(my_fn())
    # res1, res2 = a
    # print(res1, res1, end='\n')



    print(f'время работы процесса: {datetime.now() - start}')
